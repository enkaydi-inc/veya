{
  "name": "veya",
  "version": "1.0.0",
  "description": "wellbeing",
  "license": "MIT",
  "author": "enkaydi",
  "type": "module",
  "main": "src/main/main.js",
  "scripts": {
    "dev": "nodemon --watch src --watch public --ext js,html,css --delay 300ms --exec electron .",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^24.0.13",
    "electron": "^37.2.0",
    "nodemon": "^3.1.10"
  }
}
